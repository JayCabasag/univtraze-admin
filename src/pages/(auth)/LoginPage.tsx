import Login from '@/components/login/Login';

export default function LoginPage() {
  return (
    <main className="p-[15px] h-screen flex justify-center items-center ">
      <Login />
    </main>
  );
}
